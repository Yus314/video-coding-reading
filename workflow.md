# 更新・選別手順

## 正本と生成物

- **正本**：`papers.json`（書誌・注釈・根拠）、`relations.json`（引用/推奨順/対比）。
- **自動生成**：`annotated-bibliography.md`、`papers/Pxx.md`、`relations-view.md`。直接編集しない。
- **人が編集**：README、分野地図、読書順、系譜の考察、比較メモ、採否の考察。
- **補助データ**：`sources.json`、`search-log.json`、`screening.json`、`comparison-conditions.json`。
- **独立探索の正本**：`independent-review.json`。検索式とその探索回の採否を保持し、`independent-review.md`は手編集の解説。既存資料と同一なら`canonical_paper_id`で対応付け、P資料の現行書誌・確認状態は`papers.json`を使う。探索時の確認範囲は履歴として残す。
- **限定監査**：`evidence-audit.json`がP12監査の構造化記録、`evidence-audit.md`が手編集解説。`figure-checks.json`は実際に画像確認した図だけを列挙する。これらは論文全体の精読状態ではない。

P/S/F/ICは品質の順位ではない。新しい調査ごとに別の資料台帳を増やさず、通常の採否はscreening、独立探索の追記はindependent-reviewへ。F01/F02の案内はfollowup-review、採否はscreeningに保持する。

READMEや読書ガイドには現在の案内と根拠を記し、作業経緯・改訂回ごとの報告は載せない。変更履歴はGitで管理する。出典の版・確認日・確認範囲、探索の来歴は根拠として保持する。

個人の読書記録や非公開の取得キャッシュは生成物にも公開リポジトリにも混ぜない。論文本文を転載せずURLと節/図表の位置を残す。

## 追加・修正

1. DOI/arXiv/題名で同一成果を照合。版違いは二重計上せず、見た版を明記。
2. 通常の探索経路を `search-log.json`、具体的な採否を `screening.json` に記録。既存名簿を伏せる独立探索は `independent-review.json` に検索・探索時点・照合後の採否をまとめ、入口からリンクする。引用確認を後から行っても、初回発見を引用探索に書き換えない。
3. `papers.json` に問い・読む理由・代替候補との差・確認範囲を記す。本文を読んで根拠を補強できた場合にだけ `evidence_claims` に原文URLと節/図表を追加する。
4. `selection` に採否、重要性、代替、確信を分けて記す。`prerequisites` は推奨順、`required_background` は概念上の前提であり混同しない。
5. 必要な `relations.json` と手編集の地図/経路/比較メモを更新する。読む候補の追加が共通キューの増大を意味しないようにする。
6. 地図・経路・カード・比較メモに判断変更を反映する。未確認事項には「保留になる結論／それでも言えること／次に必要な証拠」を添え、用途と無関係な再現を読書の前提にしない。次を実行し、差分を確認して公開する。

```sh
python3 scripts/catalog.py build
python3 scripts/catalog.py check
python3 -m unittest discover -s tests -v
```

`check` は生成物を書き換えず、データ不整合と生成差分を検出する。詳しくは [開発手順](DEVELOPMENT.md)。テスト成功は注釈の内容が正しい証明ではなく、重要な主張は元論文で別途確認する。

## 探索と推薦を分ける

候補発見、主張の根拠評価、次の読書作業の推薦は別に判断する。既知論文の所在検索を未知候補発見と数えず、概念・制約・失敗からの検索、会場/規格の入口、異なる種からの引用探索を必要に応じて組み合わせる。複数エージェントの一致は統計的な独立性や網羅性の証拠ではない。

検索ログには実行した式・取得範囲・選んだ候補・停止理由を残す。検索結果の本文を全部見ていない場合は明示し、未点検の結果を不適格と数えない。PRISMA等の記載項目は透明性の参考であり、適合だけで検索の完全性や教材の学習価値を保証しない。[PRISMA 2020 Scope](https://www.bmj.com/content/372/bmj.n71)。

## 採否と停止

被引用数・掲載先・BD値を総合スコアにしない。概念の役割、現在の集合への追加価値、代替、実験への確信を分ける。保留は質の否定ではない。

- **追加する**：既存集合にない概念・制約・重要な反証に答え、対象読者と読む箇所を説明できる。新着・高被引用だけでは追加しない。
- **置き換える**：同じ到達目標の代替資料を実際に確認し、何が得られ、何を失うか説明できる。原著の歴史的重要性だけで入口教材に固定しない。所要時間未測定なら「速く読める」と断定しない。
- **推薦から外して参照用にする**：目的に対して重複する、または別経路で必要な概念を得られる。資料庫からの削除や論文の低品質判定とは分ける。
- **保留する**：本文不足、比較条件不足、追加価値不明。次の判断に必要な箇所・条件を採否記録へ残す。
- **再検討する**：重要な訂正・撤回・版差分、根拠の誤読、より適合する代替、読者が止まる箇所の具体的証拠が得られた時。全出典の訂正・撤回状態を一括確認済みとは扱わない。

検索を止める理由（問いに必要な候補が得られた、予算を使った、局所的に新規候補が減った）と、読書を終える条件（目的の問いを説明できる）は分ける。固定回数や候補数を普遍的な停止基準にしない。全会議・引用網の探索完了を、単に追加候補が減ったことから主張しない。

新着の自動採用や定期巡回はない。判断変更の理由はコミットに記し、案内には現在の選定理由を示す。

## 案内の評価

- **構造・事実**：生成の整合、到達先、原文の支持箇所・版・条件を点検する。リンク成功や原文との文字列一致だけでは主張の正しさを保証しない。
- **使いやすさ**：目的に合う入口を選べるか、読む箇所・戻り先・終了条件が分かるかを確認する。編集者の机上点検と利用者の観察を区別する。
- **学習**：理解・保持・別論文への応用は別の確認対象。クリック・既読・満足度を代用しない。現在、人間の読書試行は未実施。

人間の試行を行う場合だけ、同じ目的・読書予算で、例えば「概説の指定節だけ」と「案内に従う」を比較する。選書、順序、解説を全部変えた比較から、順序だけの効果を断定しない。小さな試行はつまずきの発見には使えるが、普遍的な教育効果の証明ではない。個人の回答・読了記録を公開ファイルへ入れない。試行や評価基盤の整備を、明白な誤読・冗長さの修正の前提にしない。
