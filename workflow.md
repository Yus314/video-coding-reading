# 更新・選別手順

この初期版はファイルベース。自動収集・定期実行は設定していない。

## 追加

1. `search-log.json` に検索語と経路を追加。既存DOI/arXiv/題名を照合し、同一成果の版違いを別成果として重複登録しない。
2. 公式出版社・会議・著者・arXivのページを確認。`sources.json` に原文URLを記録し、第三者本文の取得キャッシュは公開対象にしない。二段組PDFの抽出乱れは元PDFで確認する。
3. `papers.json` に、問い・役割・前提・既存論文との差・確認範囲を明示。性能主張には条件と根拠を追加。
4. 読む理由が重複するなら候補のまま保留し、キューを増やさない。除外なら理由を残す。
5. `annotated-bibliography.md` と `papers/Pxx.md`、影響する地図・キュー・関係を更新。JSONとMarkdownの自動同期はないため両方を更新する。
6. 本人の既読状態・感想は本人の申告以外で確定しない。

## 優先順位

関連性、概念の新規性、前提としての価値、現在の集合との差、根拠の確認可能性を別々に判断。被引用数・掲載先・BD値だけで自動順位を作らない。理論価値と実験への確信は分離。

## 現時点の保留・非採用

- 同一成果のarXiv/会議/刊行版：別カウントせず、対応と差分未確認を記録。
- タイトルにない用途の強い性能一般化：採用しない。
- 追加の同系統DCVC改良群：P12の実用性を優先し、網羅追加は保留。
- 周辺の光フロー・一般動画生成：圧縮への役割が明確になるまで対象外。

具体的な論文を審査して除外した完全ログではない。初回は採用種と領域ギャップの記録が中心。

## 完了と継続を分ける

初期版の受入条件は、候補・地図・小さな順序・関係・比較メモが実在し、ID/リンク/引用抜粋が整合すること。分野の網羅・最新探索・全件精読・実験追試は別作業。次の追加は上記ギャップか読書で発生した問いに沿わせる。
