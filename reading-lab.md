# 実際に読む：復号器は何を知り、何を受け取るか

この経路の到達目標は「新しいcodecの図を見て、何を送り、復号側で何を作り、何を次フレームへ持ち越すか説明する」こと。全論文読破でも性能順位の決定でもない。利用者の読了記録は含まない。

## 最初の作業：一つの図を六つの問いで読む

最初はP02 II-A/Fig.1（[従来型の案内](conventional-reading.md)）か、目的に合う下記の図を一つ選ぶ。全経路を順番に消化する必要はない。

1. **符号化側だけが知るものは？** 原画像を復号器も持つと仮定していないか。
2. **何を送る？** 動き・残差latent・副情報・モード等を区別する。この論文での記号の定義を確認し、別論文のz等の意味を持ち込まない。図の矢印すべてが通信ではなく、図にない副情報の費用も本文で確認する。概念的な送信対象と物理的なbitstream本数は別。
3. **復号側で何を生成する？** 既復号画像・特徴・受信情報から作る予測や確率分布のパラメータを挙げる。
4. **何を次へ保持する？** 再構成画像、特徴状態、確率モデルの文脈を区別する。次フレームに意味的に必要な状態と、再計算可能な作業領域・実装キャッシュも分ける。状態を使わない静止画方式なら「フレーム間状態なし」と答えてよい。
5. **どの順に復号する？** 先に必要な情報をたどり、必要な部分順序を矢印にする。時間方向の依存と同一フレーム内の逐次依存を分ける。符号化順序・物理的な格納順・並列実行の可否を同じ一列の順番と決め付けない。
6. **図だけでは何が分からない？** ビットの実計測範囲、初期フレーム、モデルの配布・更新、計時範囲などは本文・仕様・設定へ戻す。不明を「送らない」「存在しない」と言い換えない。

各信号に「送る／既復号情報から生成／符号化側のみ／事前共有」を付け、根拠の節と不明点を一つずつ示せれば一区切り。モデル重みや設定が事前共有されることと、個々の動画で無料に変更できることは別である。

### 最小の確認用解説（自分の説明の後に読む）

- **従来型の閉ループ**：符号化側の原画像をそのまま参照として渡さず、局所復号で受信側と整合する再構成参照を作る。動きやモードを選ぶ探索と、復号器が実行すべき処理は別。[P02 II-A/Fig.1](conventional-reading.md)。
- **P07の副情報**：受信したhyperlatentから確率分布のパラメータを生成し、それで画像latentを復号する。確率パラメータとhyperlatentを、同じ送信対象と数えない。副情報自身の符号量は必要。§3/Fig.4。
- **P08とP10の違い**：P08では動きから予測を作り残差を復元する経路を追う。P10では特徴contextの入力先を分け、符号化器・復号器・確率モデルのどこで使うかを確認する。contextを原画像から無料で得ると解釈しない。P08 §3.2/Fig.2、P10 §3.1–3.3/Fig.2/4。

これは原論文の代わりになる完全な解説ではない。初期化や各方式固有の送信単位は該当本文に戻る。性能順位はこの作業だけでは決められない。

## 入口：分かるものは飛ばす

- ハイブリッド符号化の再構成ループを説明できる → P01は参照用。分からなければ [P01](papers/P01.md) の符号化構成図とVCL説明へ。
- 予測誤差だけでなく動き・モードの符号量も選択に効くと説明できる → P06は参照用。分からなければ [P06](papers/P06.md) のVideo Compression Basics / Variable Block Sizesへ。
- 量子化とentropy modelとentropy coderを区別できない → S03 §3.2.1を先に読む。VAE全般を先に学び終える必要はない。

[S03確認版](https://arxiv.org/pdf/2202.06533v3.pdf)。§3.2.1 / §3.3.5を確認済み。全章精読ではない。

## 1. 副情報で確率モデルを変える

**資料**：[P07 Hyperprior](papers/P07.md) §3 / Fig.3–4。確率モデルの背景が不足する場合はS03 §3.3.5を先に使う。

- **読む仕事**：画像latentとhyperlatentを分け、復号順序を追う。
- **先に飛ばす**：性能曲線の細かな比較、すべての学習実装詳細。性能を引用する段階では戻る。
- **飛ばせる条件**：副情報を先に復号する理由と、その符号量も費用に含むことを説明できる。
- **次へ進める条件**：受信側が原画像を使わず、受信した情報から同じ確率を得る流れを指で追える。
- **戻る先**：rateと符号長の関係はS03 §3.2.1、確率モデルは§3.3.5。量子化の学習方法そのものが問いなら§3.3へ進むが、この短い経路では必須にしない。

**代替判断**：概念だけならS03の該当節で代替できる。P07は具体的な構成図・副情報の費用を原著で追う役割として維持し、全編精読は要求しない。

**原図から追う**：Fig.3の`U|Q`を二つの処理の直列実行と読まない。Fig.4では受信側の`zのAD → h_s → sigma → yのAD → g_s`を追う。画像側と副情報側の二つのbitstreamを区別する。両図の画像を確認済み。

## 2. 動きと残差の情報経路を追う

**資料**：[P08 DVC](papers/P08.md) §3.2 / Fig.2、§3.6。

- **読む仕事**：動き、再構成した参照、残差、各々の符号量がどこへ入るかを確認する。
- **先に飛ばす**：全ベンチマーク表。構造を説明するだけなら不要。
- **飛ばせる条件**：従来型と対応させ、原フレームからしか得られない情報を復号側が無料で使っていないか点検できる。
- **次へ進める条件**：動きの誤差が小さいだけで最良のcodecとは限らない理由を、符号量も含めて説明できる。
- **戻る先**：予測ループはP01、選択の費用はP06。

**代替判断**：P18にも動画圧縮の設計があるが、従来型の動き＋残差との部品対応にはP08を使う。P18を読む前の必須精読とはしない。

## 3. 引き算以外に、条件をどこへ渡すか

**資料**：[P10 DCVC](papers/P10.md) §3.1–3.3 / Fig.1–2。確率モデルの情報経路はFig.4を参照。

- **読む仕事**：contextがencoder、decoder、entropy modelでどう使われるかを区別する。
- **先に飛ばす**：性能順位の結論。まず構造の違いを読む。
- **飛ばせる条件**：特徴条件付けと「残差を別名にしただけ」の違いを説明できる。
- **次へ進める条件**：受信側のcontextが何から作られるか説明でき、「条件付きなら実装に関係なく必ず優れる」とは結論しない。
- **戻る先**：副情報・確率モデルはP07。理論的優位性の前提が問いなら [F02の§III–IV](followup-review.md) を読む。

**代替判断**：F02は情報経路の限界を補うが、DCVCの具体的構造の教材としてP10を置き換えない。目的に応じて追加する。

**原図から追う**：Fig.4でhyperprior・spatial prior・temporal priorがprior fusionへ入り、mean/scaleをyのAE/ADへ供給する経路を確認する。auto regressiveの既復号要素への依存は本文も参照する。矢印が左から右へ流れることを、全要素の並列復号が可能という意味にしない。図画像を確認済み。

## 4. 必要な枝だけ追加する

- **状態を引き継ぐ別設計**：[P18](papers/P18.md) §2.1 / Fig.5。レート制御が目的でなければ§4は後回し。再構成画像と学習状態の違いを説明できれば一区切り。
- **長い系列での挙動**：[F01](followup-review.md) §3.3 / Fig.5–6。feature refreshとIフレーム挿入を区別する。P18→F01を直接の発明史とは扱わない。
- **実装速度**：[P12](papers/P12.md) の対象精度・ハードウェア・速度範囲へ。構造理解だけなら今は読まない。
- **性能を引用する**：[比較条件メモ](comparison-memo.md)。BDの補間が論点ならP13、動画間の平均が論点ならP14。評価二論文の精読をP12の構造理解の前提にはしない。

## 任意の理解確認

これは読了を強制する課題ではない。質問に詰まった場所を、戻る先を探す目印にする。

1. **副情報を無料としたら何を誤るか？**
   - 確認の観点：画像latentだけの減少を、総符号量の減少と取り違える。P07 §3へ。
2. **予測が完全なら、どんな条件付きネットワークでも追加情報は要らないか？**
   - 確認の観点：予測から再構成への経路が情報を保持するかを確認する必要がある。F02 §IV-Aはこの境界例を使う。
3. **feature refreshを32フレームごとに行えば、intra-periodも32か？**
   - 確認の観点：同じではない。F01 §3.3は直前の再構成画像からcontextを作り直す機構で、Iフレーム挿入とは別。

## 任意の適用例：P09で六つの問いを試す

P08の情報経路が読めた後、**予測演算の別設計を知りたい場合だけ** [P09](papers/P09.md) §3.1–3.4へ進む。P10の前提にはしない。§3.1–3.4の本文とFig.1–2の画像を確認済み。確認版はCVPR 2020会議版。[1]

### まず答えてから、以下と照合する

1. **符号化側のみ**：現在の原フレーム、量子化前のlatent、予測との差としての元の残差。前の再構成画像は復号側も使う。§3.2、Fig.2キャプション。[1]
2. **送信対象**：先頭画像latent `[z_0]`、動きlatent `[w_i]`、残差latent `[v_i]`と、それぞれのhyperprior副情報。`z_0`はこの論文ではI画像latentであり、P07のhyperlatent記号と同じ意味ではない。§3.2、§3.4。[1]
3. **復号側で生成**：動きlatentからscale-space flowを得て、前の再構成画像に対するscale-space warpingで予測を作る。**Fig.2のResidual Decoderは残差latent `[v_i]` に加えて動きlatent `[w_i]` も入力する**。その復号残差を予測に足して現在の再構成を得る。dense flowや予測画像を、それぞれ独立に別送するという解釈ではない。§3.1–3.2。[1]
4. **次へ保持**：構造上、現在の再構成画像が次の参照になる。volumeをキャッシュするかなど実装の全メモリ保持は本文から確定しない。§3.2、Fig.2キャプションに基づく区別。[1]
5. **部分順序**：`復号した動き＋前の再構成 → 予測`、`[w_i]＋[v_i] → 復号残差`、`予測＋復号残差 → 現在の再構成 → 次の参照`。残差の再構成には動きlatentが必要だが、scale-space flow生成・warping完了まで必ず待つとはこの図から断定しない。主latentのentropy decodingと、latentを入力するResidual Decoderの処理も区別する。hyperprior内の自己回帰を使わないという記述も、時間依存がない意味ではない。Fig.2、§3.2。[1]
6. **本文へ戻るもの**：副情報の費用、量子化の学習近似、scaleの再パラメータ化。物理的stream配置・実行スケジュール・実ビット範囲は未確認。§3.1–3.4。[1]

### 混同しやすい概念

- **scaleは何のscaleか**：ここでは参照のぼかしを変えたscale-spaceの座標。latentの量子化刻みや確率分布の尺度と混同しない。§3.1 Eq.(2)–(3)の`g_z=0`はbilinear warpingへ戻る例。Reparameterizationでは有効なぼかし幅からvolume座標へ変換する。[1]
- **量子化と補間を別の位置へ置く**：`[w_i]`の量子化と、復号したflowによるscale-space補間は別処理。§3.3は学習時のエントロピー推定用ノイズと後段に渡す値のstraight-through推定を併用する。P07の図をそのまま全学習方式に当てはめない。[1]
- **戻り先を増やし過ぎない**：量子化・符号長は既存S03 §3.2.1、確率モデルは§3.3.5、副情報の一般的な復号順はP07 §3へ。新しいscale-space演算だけP09 §3.1を読む。他の論文の通読は前提にしない。

**終了条件**：上の記号を送信・生成・保持へ分類し、量子化とscale-space補間を別の段階に置ける。物理的stream順序が未確認でも、構造理解は進めてよい。実装・並列性の判断には実装又は仕様が必要、性能順位には同条件の実験が必要であり、この練習だけでは保留する。

**この例の位置付け**：原文に基づく編集者の解説であり、学習効果を検証した試験ではない。

## この経路の検証範囲

出典箇所、導線、条件変更時の戻り先を机上で点検し、P07 Fig.3–4、P10 Fig.4、P09 Fig.1–2は画像として確認した。他の全図を視覚点検したわけではない。[確認範囲](figure-checks.json)。人が実際に読んだ際の所要時間・理解度・つまずきは未測定。上の観点は編集者の学習案内であり、利用者自身の回答・読了記録ではない。

## Sources

[1] https://openaccess.thecvf.com/content_CVPR_2020/papers/Agustsson_Scale-Space_Flow_for_End-to-End_Optimized_Video_Compression_CVPR_2020_paper.pdf — P09 official CVPR 2020 paper
